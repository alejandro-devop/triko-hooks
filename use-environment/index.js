import {
  ENV,
  APP_VERSION,
  APP_PACKAGE_ANDROID,
  APP_PACKAGE_IOS,
  APP_CLIENT_ANDROID,
  APP_CLIENT_IOS,
  APP_TRIKOWORK_ANDROID,
  APP_TRIKOWORK_IOS,
  GRAPHQL_SERVER_SECURE,
  GRAPHQL_SERVER,
  PUSHER_CHANNEL_AUTH_URL,
  PUSHER_CHANNEL_PREFIX,
  BUCKET_BASE_URL,
  PUSHER_ID,
  PUSHER_KEY,
  PUSHER_SECRET,
  PUSHER_CLUSTER,
  ERROR_REPORTER_URL,
  TRANSLATION_GROUP,
  GOOGLE_API_KEY,
  APP_CODE,
  IP_LOCALIZER,
} from 'react-native-dotenv';

const useEnvironment = () => {
  return {
    ENV,
    APP_VERSION,
    APP_TRIKOWORK_ANDROID,
    APP_TRIKOWORK_IOS,
    APP_PACKAGE_ANDROID,
    APP_PACKAGE_IOS,
    APP_CLIENT_ANDROID,
    APP_CLIENT_IOS,
    GRAPHQL_SERVER_SECURE,
    GRAPHQL_SERVER,
    PUSHER_CHANNEL_AUTH_URL,
    PUSHER_CHANNEL_PREFIX,
    BUCKET_BASE_URL,
    PUSHER_ID,
    PUSHER_KEY,
    PUSHER_SECRET,
    PUSHER_CLUSTER,
    ERROR_REPORTER_URL,
    TRANSLATION_GROUP,
    GOOGLE_API_KEY,
    APP_CODE,
    IP_LOCALIZER,
  };
};

export default useEnvironment;
